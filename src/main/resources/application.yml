server:
  port: 9090
spring:
  application:
    name: docker-jenkin-springboot
  datasource:
    url: jdbc:mysql://localhost:3306/ust1
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true