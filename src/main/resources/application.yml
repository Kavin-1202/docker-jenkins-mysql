server:
  port: 9090
spring:
  application:
    name: docker-jenkin-springboot
  datasource:
    url: jdbc:mysql://db:3306/springboot
    username: root
    password: pass@word1
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true